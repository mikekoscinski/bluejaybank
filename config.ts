require('dotenv').config()

// TODO: Replace credentials below with those from .env

module.exports = {
  user: 'your_db_username',
  host: 'your_db_host',
  database: 'your_db_name',
  password: 'your_db_password',
  // port: your_db_port, // (usually 5432)
  port: 5432, // (usually 5432)
};